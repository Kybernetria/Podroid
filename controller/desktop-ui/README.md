# Desktop UI

Owns the future Slint presentation adapter over controller core. It contains interaction and display logic only; protocol, credentials, retries, and state refresh remain in controller core.

Closing the UI must not stop configured phones or guest workloads.
