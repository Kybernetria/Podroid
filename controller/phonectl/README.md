# phonectl

Owns the future command-line adapter over controller core. Commands map to explicit management operations and produce stable machine-readable outcomes where appropriate.

It does not execute arbitrary Android commands or schedule Swarm workloads.
