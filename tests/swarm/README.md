# Swarm Acceptance Tests

Future evidence that Docker Swarm runs and reconciles inside guests over the workload plane, including controller absence, node reconnect, duplicate bootstrap, and dependency failure.

Tests must also prove that the APK does not become the workload scheduler. No test task is added in Milestone 1.
