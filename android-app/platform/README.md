# Android Platform

Owns future adapters for Android APIs, permissions, secure storage, foreground execution, and official Tailscale/libtailscale network hooks. It does not own management semantics, VM policy, or guest networking.

Current Android integration remains in `app/`; this area is not built yet.
