# Android UI

Owns future Android presentation and user input. It observes authoritative service state and submits intents; it does not launch QEMU directly, schedule workloads, or become required for unattended operation.

Current Compose UI remains in `app/`; this area is not built yet.
