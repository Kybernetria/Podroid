# Restricted SSH Server

Owns future SSH authentication, channel/session bounds, deadlines, teardown, and dispatch into the management protocol. It exposes no host shell, arbitrary exec, arbitrary files, agent forwarding, or unrestricted port forwarding.

No server is implemented or enabled in Milestone 1.
