# Management

Owns the versioned restricted host-management protocol and its SSH exposure. It may request authorized Android-host lifecycle effects but provides no general shell and owns no guest workload state.

Milestone 1 contains README skeletons only.
