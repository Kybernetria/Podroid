# Management Protocol

Owns future versioned, bounded request/response types and explicit error semantics for authorized host management. Unknown versions and operations fail closed.

It excludes arbitrary commands, file access, QMP passthrough, workload scheduling, and transport-provider details.
