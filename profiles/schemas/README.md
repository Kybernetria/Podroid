# Profile Schemas

Owns future versioned, bounded, distro-neutral schemas for guest artifacts, boot inputs, capabilities, and hooks. Parsers must reject unknown incompatible versions, unsafe paths, excess sizes, and invalid cross-field combinations before effects.

No schema is defined in Milestone 1.
