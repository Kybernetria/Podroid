# Transport API

Owns future provider-neutral interfaces for bounded authenticated connections, deadlines, cancellation, peer identity evidence, and teardown. The API carries management protocol bytes but grants no management authority.

No transport interface is implemented in Milestone 1.
