# VM Storage

Owns future validated image paths, attachment plans, capacity bounds, integrity checks, and storage cleanup. It does not interpret guest package state or expose arbitrary host paths.

Existing persistence paths remain authoritative until a reviewed migration.
