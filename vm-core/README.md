# VM Core

Owns future distro-neutral VM model, lifecycle, QEMU, QMP, and storage contracts. It must not depend on Android UI, controller presentation, a distribution name, or workload scheduling.

Milestone 1 contains ownership documentation only; current VM code remains under `app/`.
